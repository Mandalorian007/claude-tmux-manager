# pnpm configuration for claude-tmux-manager

# Use pnpm's strict peer dependencies
strict-peer-dependencies=true

# Auto install peer dependencies
auto-install-peers=true

# Save exact versions (no ^ or ~)
save-exact=true

# Use hoisted node_modules for better compatibility
node-linker=hoisted

# Registry configuration
registry=https://registry.npmjs.org/

# Enable package import maps for better resolution
enable-pre-post-scripts=true

# Store location (optional - uses default if not specified)
# store-dir=~/.pnpm-store